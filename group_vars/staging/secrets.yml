# Staging secrets - encrypt with ansible-vault
# ansible-vault encrypt group_vars/staging/secrets.yml

# Database credentials
db_password: "{{ lookup('env', 'STAGING_DB_PASSWORD') }}"

# API Keys
anthropic_api_key: "{{ lookup('env', 'STAGING_ANTHROPIC_API_KEY') }}"
openai_api_key: "{{ lookup('env', 'STAGING_OPENAI_API_KEY') }}"
gemini_api_key: "{{ lookup('env', 'STAGING_GEMINI_API_KEY') }}"

# JWT
jwt_secret: "{{ lookup('env', 'STAGING_JWT_SECRET') }}"

# SSH Keys (if needed)
# ssh_private_key: "{{ lookup('file', '~/.ssh/id_rsa') }}"